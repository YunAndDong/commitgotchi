# Spring Framework

## JPA

JPA maps Java objects to relational tables. Lazy loading, fetch join, EntityGraph, ORM,
Hibernate persistence context, transaction boundaries, and N+1 problems are key interview topics.
