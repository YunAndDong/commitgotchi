# Network

HTTP is an application protocol over TCP. It uses methods, status codes, headers, cache
control, authentication, authorization, CORS, and REST constraints to build APIs.

| Method | Meaning |
| --- | --- |
| GET | read resource |
| POST | create resource |

See [REST constraints](https://example.com/rest) and use `Cache-Control` carefully.
