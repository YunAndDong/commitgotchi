This document intentionally has no ATX heading.
It still has enough content about operating system process, thread, memory, scheduling,
context switching, and concurrency concepts to become a concept chunk with a fallback heading.
