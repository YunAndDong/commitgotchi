# Decision Log — commitgotchi PRD

> Canonical memory and audit trail. 모든 결정·변경·오버라이드를 시간순으로 기록.

## 2026-06-07

- **워크플로 시작.** Intent: Create (기존 PRD 없음). 입력: brief-commitgotchi-2026-06-07 (brief.md + addendum.md). Working mode: **Fast path** (사용자 선택).
- **결정 D1 — 퀴즈 채점 시점.** 옵션(즉시 동기 / 자정 배치 SQS / 둘 다) 중 **자정 배치(SQS) 채점** 선택. 애드덤 Open Question 1 해소. → FR 및 일일 레포트 흐름에 반영. 즉시 채점은 v2 후보.
- **결정 D2 — 점수 누적 모델.** **일 단위 누적** 선택. 매일 산출된 점수 변화량을 활성 캐릭터에 즉시 누적. 주간 변화량(weeklyScoreChanges)은 표시·통계·AI 컨텍스트 용도로만 집계. 애드덤 Open Question 2 해소.
- **결정 D3 — 진화 규칙 구체값.** 사용자 위임 → 제가 합리적 기본값 제안([ASSUMPTION] 태그). 능력치 총합 1,000점 도달 시 1회 진화, 이미지 교체 + 능력치 보너스 규칙을 PRD에 제안값으로 명시. 검토 후 수정 가능.
- **결정 D4 — 채점 트리거 확정에 따른 후속.** 퀴즈 제출은 답안 저장만 동기 처리, AI 채점·피드백은 자정 배치에서 일괄 수행하도록 FR 정렬.
- **마감(Finalize).** Fast path 초안 완성. prd.md(FR-1~23, UJ-1~3, SM-1~3 + 카운터 SM-C1/C2), addendum.md, decision-log 작성. 정합성 점검: FR 번호 연속·UJ/SM 참조·Glossary 용어 일관 확인. 미해결 6건은 §8 Open Questions로 명시 이관. prd.md `status: final` 처리. 다음 단계 후보: bmad-ux, bmad-create-architecture, bmad-create-epics-and-stories.

## 2026-06-11

- **업데이트 시작.** Intent: Update. 변경 목적: Spring Boot 인증·인가 기능을 전체 MVP 중 가장 먼저 구현할 수 있도록 기존 FR-1 회원가입과 FR-2 로그인/JWT를 구체화.
- **결정 D6 — 첫 번째 구현 증분 범위.** 전체 MVP 범위는 유지하되 첫 구현 증분은 인증·인가로 제한한다. 이 증분에서는 `users`, `refresh_tokens` 테이블만 생성하고 캐릭터·리포트·퀴즈·게시판·Spring AI 및 전체 도메인 테이블 생성은 제외한다. 변경 이유: 인증 기반을 먼저 종단 구현·검증하기 위함.
- **결정 D7 — 공개 회원가입 Role 정책.** 공개 회원가입은 항상 `USER`를 생성하고 Role 선택 및 `ADMIN` 생성을 금지한다. 초기 `ADMIN` 프로비저닝 방식은 미해결 항목으로 남긴다.
- **결정 D8 — JWT/Refresh Token 정책.** 짧은 유효기간의 서명된 Access Token과 해시 저장·Rotation·폐기 관리되는 Refresh Token을 사용한다. 로그아웃은 제출된 Refresh Token을 폐기하며 Access Token 블랙리스트는 첫 증분에서 제외한다.
- **결정 D9 — 인증 실패 계약.** 인증 실패 `401`, 권한 부족 `403`, 이메일 중복 `409`, 입력값 검증 실패 `400`을 명확히 구분하고, 일관된 오류 응답에서 비밀번호·토큰 원문·내부 예외 상세를 노출하지 않는다.
- **결정 D10 — FR 식별자 안정성.** 기존 FR-3~23의 참조를 깨지 않기 위해 FR-1/FR-2를 상세화하고 새 인증 요구사항을 FR-24~28로 추가한다.
- **미해결/가정 기록.** Access/Refresh Token 정확한 유효기간, JWT 서명 알고리즘, 운영 비밀키 최소 길이, 초기 `ADMIN` 프로비저닝 방식은 아키텍처 단계 결정 대상으로 표시했다.
- **결정 로그 감사.** 이번 인증 변경의 D6~D10은 PRD/addendum에 반영됨을 확인했다. 기존 이력에서 D1/D4의 자정 배치 표현과 현재 PRD/addendum의 즉시 채점 개정 내용이 불일치하고 addendum의 D5가 canonical log에 누락된 점을 발견했다. 인증 범위 밖의 기존 제품 결정을 임의 변경하지 않고 `review-rubric.md`의 후속 정리 항목으로 남겼다.
- **업데이트 마감(Finalize).** 사용자 입력 대조(`reconcile-auth-update.md`)와 품질 리뷰(`review-rubric.md`)를 완료했다. 인증 관련 차단 이슈 없이 `prd.md` 상태는 `final`, `updated`는 `2026-06-11`로 유지한다.
